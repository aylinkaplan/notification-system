# First-time setup: cp .env.example .env
# Docker Compose reads .env automatically.
# For go run: export $(cat .env | xargs) && go run ./cmd/api

PORT=8080
DATABASE_URL=postgres://postgres:postgres@localhost:5432/notifications?sslmode=disable
RABBITMQ_URL=amqp://guest:guest@localhost:5672/
WEBHOOK_BASE_URL=https://webhook.site
WEBHOOK_UUID=c56aef09-6efd-405a-ac0e-169b3544e1cd
